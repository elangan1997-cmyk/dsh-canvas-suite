# 网吧/新电脑验收清单

在没有开发工具、没有管理员权限的 Windows 10/11 x64 账户上执行：

1. 双击 `DSH-Setup-x64-v*.exe`，安装目录保持默认，确认不弹管理员权限错误。
2. 安装完成后首次启动，确认 DSH 窗口、画布入口和项目入口可点击。
3. 新建项目，打开项目文件夹；在外部复制一张 PNG/JPG/SVG/PDF/AI，确认画布自动发现新增文件。
4. 打开 PNG 编辑/擦除；确认原图清晰、导出尺寸比例不变、失败时日志有步骤和错误码。
5. 框选文字并识别；确认识别结果默认勾选、PSD 中有文字图层和背景图层。
6. 执行去背景、转矢量、PDF/AI 预览；确认成功或显示明确的“运行时缺失/Adobe 未安装”原因。
7. 在聊天中生成图片并加入画布；确认原图落在项目根目录的 `DSH聊天生成图片`，画布删除不影响聊天原图。
8. 点击“更多 → 操作日志”，确认能看到启动、导入、AI、导出和失败步骤。
9. 退出后重启 DSH，确认最近项目和画布内容仍在；删除画布图片后确认对应文件进入画布回收站。
10. 运行环境医生，确认报告保存到 `%LOCALAPPDATA%\DSH\Logs\environment.json`。

记录：Windows 版本、安装包 SHA-256、是否代理、每项 PASS/FAIL、`%LOCALAPPDATA%\DSH\Logs` 及安装器日志。

