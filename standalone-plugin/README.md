# DSH画布工作台

这是给已有 DSH Desktop 用户的独立画布插件包。先完全退出 DSH Desktop，再运行：

- Windows：双击 `install-windows.cmd`。
- macOS：执行 `bash install-macos.sh`。

完整功能、环境要求和回滚说明见仓库中的 `docs/DSH-CANVAS-WORKBENCH-WINDOWS.md` 与 `docs/DSH-CANVAS-WORKBENCH-MACOS.md`。安装脚本只更新 `@local/canvas-workbench`，不携带账号、API Key、项目或画布快照。
