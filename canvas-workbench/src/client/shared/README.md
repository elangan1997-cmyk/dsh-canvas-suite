# client/shared/

`components/`（Modal / Toast / Toolbar / Lightbox）、`hooks/`、`utils/`（`imageUrl/previewUrl/displaySourceUrl` 255-286、`basename` 426、`dataURLToFile` 456）。样式按 Feature 拆分，禁止单个巨型 global.css（§23）。
