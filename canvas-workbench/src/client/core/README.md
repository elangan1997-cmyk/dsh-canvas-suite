# core/

`canvas/`（iframe 桥、srcdoc 源码、postMessage 协议）、`document/`、`selection/`、`commands/`（Phase 6 CommandBus）、`history/`（Undo/Redo）、`assets/`、`jobs/`、`capabilities/`（现 `compatibilityLogger/safeEffect/safeSlot` 4389-4424）。
