# services/

`project-store.js`（canvas.json / .dsh-canvas-state.json / 项目目录）、`material-library.js`（颜色标签 `readMaterialTags/writeMaterialTags/tagsForDirectory` 213-243、尺寸探测 `probeMaterialSize` 194-212、目录读取）、`text-service.js`（`analyzeTextWithCurrentModel` 335-366、`visionBlocks` 302-334）、`generation-service.js`、`export-service.js`、`capability-service.js`。
