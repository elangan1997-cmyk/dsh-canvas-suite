# adapters/

`dsh-llm.adapter.js`（ctx.llm.prepareCall 封装）、`attachment.adapter.js`、`python.adapter.js`（Python Tool Registry，§20：`pythonTools.register({id, script})` / `run(id, input)`）、`platform.adapter.js`（现 `lib/platform.js`，保持 `platformCapabilities()` 集中判断 OS）。
